i liked the little archietcture diagram in the architetture file, that was super cear, can you add that back and put it above the what it writes section? also there are two files called adtopt which is super confusing. call it like adopt status or somethign appropriate. also, how does this affect existing users using entire when this is rolled out?